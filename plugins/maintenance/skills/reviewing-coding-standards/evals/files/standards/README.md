# Coding standards

`MUST` and `MUST NOT` are requirements. `SHOULD` and `SHOULD NOT` are
recommendations.

When rules conflict, local topic rules take precedence over mechanical tool
configuration, which takes precedence over explicitly adopted external
baselines.

- [Python rules](python.md)

