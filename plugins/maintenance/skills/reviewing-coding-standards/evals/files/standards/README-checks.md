# Coding standards

`MUST` and `MUST NOT` are requirements. `SHOULD` and `SHOULD NOT` are
recommendations.

Precedence is local topic rules, then mechanical tool configuration, then
explicitly adopted external baselines.

- [Configured checks](checks.md)

