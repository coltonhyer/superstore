# Coding standards

`MUST` and `MUST NOT` are requirements. `SHOULD` and `SHOULD NOT` are
recommendations.

Precedence is local topic rules, then mechanical configuration, then explicitly
adopted external baselines.

- [Scoped Python naming rules](python-scope.md)

