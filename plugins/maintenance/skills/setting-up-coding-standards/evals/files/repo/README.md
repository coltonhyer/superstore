# Parcel labels

A small Python 3.11 library used by the warehouse batch importer.
Run checks with `python3 -m unittest discover -s tests`.
Optional developer checks: `ruff check .` and `ruff format --check .`.
Release contact: warehouse-ops@example.invalid.
