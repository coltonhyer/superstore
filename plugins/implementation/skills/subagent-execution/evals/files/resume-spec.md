# Resume spec

`RESUME-1` returns `one` from `resume_one.value()`. `RESUME-2` returns `two`
from `resume_two.value()`. Keep their disjoint file-scoped evidence distinct
when reconciling a completed task from a reported task awaiting review.
