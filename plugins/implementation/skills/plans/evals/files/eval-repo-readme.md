# Release and team services

Run focused Python checks with:

```bash
python3 -m unittest discover -s tests
```

Keep browser actions in `web/`, request helpers in `app/`, and release command
entry points in `src/commands/`.
